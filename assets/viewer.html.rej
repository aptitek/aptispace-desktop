--- assets/viewer.html
+++ assets/viewer.html
@@ -178,11 +190,21 @@
         try {
             if (window.parent && window.parent !== window) {
                 const parentStyle = window.parent.getComputedStyle(window.parent.document.documentElement);
+                const parentBodyStyle = window.parent.getComputedStyle(window.parent.document.body);
                 const root = document.documentElement;
+                
+                // Inherit computed page background and text colors
+                const bg = parentBodyStyle.backgroundColor;
+                const fg = parentBodyStyle.color;
+                if (bg) root.style.setProperty('--bg-color', bg);
+                if (fg) root.style.setProperty('--text-color', fg);
+
+                // Sync link color if bootstrap sets it
+                const linkColor = parentStyle.getPropertyValue('--bs-link-color');
+                if (linkColor) root.style.setProperty('--link-color', linkColor);
+
                 const varsToSync = [
-                    '--bg-color', '--text-color', '--border-color',
-                    '--link-color', '--success-color', '--error-color',
-                    '--font-code'
+                    '--border-color', '--success-color', '--error-color', '--font-code'
                 ];
 
                 varsToSync.forEach(v => {
@@ -305,6 +327,7 @@
         } else {
             if (mode === 'render') {
                 // Static Render Mode
+                document.body.classList.add('is-render-mode');
                 codeEl.parentElement.style.display = 'none';
                 btn.style.display = 'none';
                 renderEl.style.display = 'block';
